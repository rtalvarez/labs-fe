/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { CapturePatientComponent } from './capture-patient.component';

describe('Component: CapturePatient', () => {
  it('should create an instance', () => {
    let component = new CapturePatientComponent();
    expect(component).toBeTruthy();
  });
});
